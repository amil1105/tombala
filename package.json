{
  "name": "game-center-tombala",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --stream",
    "build": "lerna run build",
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^8.0.2"
  }
} 